import { validateAnswer } from "./script.js";

describe("Answer validation tests", () => {
  test("should return array with correct positions", () => {
    expect(validateAnswer(currentWord, [A, A, B, B, C])).toBe();
  });

  test("should return array with correct positions", () => {
    expect(validateAnswer(currentWord, [A, A, B, B, C])).toBe();
  });
});
